# frozen_string_literal: true

require "bundler/gem_tasks"

# Load rake tasks from lib/tasks so custom tasks like
# lib/tasks/test_transaction.rake are available when running `rake --tasks`
Dir.glob(File.join(__dir__, "lib", "tasks", "**", "*.rake")).each { |r| load r }
